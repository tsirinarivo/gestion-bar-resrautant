# Stack nginx pour le tenant ${TENANT_SLUG}
# Généré par deploy/new-tenant.sh — certbot --nginx complète le bloc SSL

# Client public (commande en ligne)
server {
    listen 80;
    listen [::]:80;
    server_name ${TENANT_SUBDOMAIN}.sakafio.mg;

    location / {
        proxy_pass         http://127.0.0.1:${TENANT_CLIENT_PORT};
        proxy_http_version 1.1;
        proxy_set_header   Upgrade $http_upgrade;
        proxy_set_header   Connection "upgrade";
        proxy_set_header   Host $host;
        proxy_set_header   X-Real-IP $remote_addr;
        proxy_set_header   X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header   X-Forwarded-Proto $scheme;
        proxy_cache_bypass $http_upgrade;
        client_max_body_size 10M;
    }
}

# Admin (back-office manager)
server {
    listen 80;
    listen [::]:80;
    server_name admin-${TENANT_SUBDOMAIN}.sakafio.mg;

    location / {
        proxy_pass         http://127.0.0.1:${TENANT_WEB_PORT};
        proxy_http_version 1.1;
        proxy_set_header   Upgrade $http_upgrade;
        proxy_set_header   Connection "upgrade";
        proxy_set_header   Host $host;
        proxy_set_header   X-Real-IP $remote_addr;
        proxy_set_header   X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header   X-Forwarded-Proto $scheme;
        proxy_cache_bypass $http_upgrade;
        client_max_body_size 10M;
    }
}

# POS
server {
    listen 80;
    listen [::]:80;
    server_name pos-${TENANT_SUBDOMAIN}.sakafio.mg;

    location / {
        proxy_pass         http://127.0.0.1:${TENANT_POS_PORT};
        proxy_http_version 1.1;
        proxy_set_header   Upgrade $http_upgrade;
        proxy_set_header   Connection "upgrade";
        proxy_set_header   Host $host;
        proxy_set_header   X-Real-IP $remote_addr;
        proxy_set_header   X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header   X-Forwarded-Proto $scheme;
        proxy_cache_bypass $http_upgrade;
    }
}

# KDS
server {
    listen 80;
    listen [::]:80;
    server_name kds-${TENANT_SUBDOMAIN}.sakafio.mg;

    location / {
        proxy_pass         http://127.0.0.1:${TENANT_KDS_PORT};
        proxy_http_version 1.1;
        proxy_set_header   Upgrade $http_upgrade;
        proxy_set_header   Connection "upgrade";
        proxy_set_header   Host $host;
        proxy_set_header   X-Real-IP $remote_addr;
        proxy_set_header   X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header   X-Forwarded-Proto $scheme;
        proxy_cache_bypass $http_upgrade;
    }
}

# API + Socket.io
server {
    listen 80;
    listen [::]:80;
    server_name api-${TENANT_SUBDOMAIN}.sakafio.mg;

    location /socket.io/ {
        proxy_pass         http://127.0.0.1:${TENANT_API_PORT};
        proxy_http_version 1.1;
        proxy_set_header   Upgrade $http_upgrade;
        proxy_set_header   Connection "upgrade";
        proxy_set_header   Host $host;
        proxy_set_header   X-Real-IP $remote_addr;
        proxy_set_header   X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header   X-Forwarded-Proto $scheme;
        proxy_read_timeout 300s;
        proxy_send_timeout 300s;
        proxy_cache_bypass $http_upgrade;
    }

    location / {
        proxy_pass         http://127.0.0.1:${TENANT_API_PORT};
        proxy_http_version 1.1;
        proxy_set_header   Host $host;
        proxy_set_header   X-Real-IP $remote_addr;
        proxy_set_header   X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header   X-Forwarded-Proto $scheme;
        proxy_read_timeout 60s;
        client_max_body_size 20M;
    }
}
