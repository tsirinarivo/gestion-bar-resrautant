# Tenant: ${TENANT_SLUG}
# Généré par deploy/new-tenant.sh — ne pas éditer à la main
# Doubles guillemets pour supporter les valeurs avec espaces (TENANT_NAME).
TENANT_SLUG="${TENANT_SLUG}"
TENANT_NAME="${TENANT_NAME}"
TENANT_DB_NAME="${TENANT_DB_NAME}"
TENANT_REDIS_DB="${TENANT_REDIS_DB}"
TENANT_API_PORT="${TENANT_API_PORT}"
TENANT_WEB_PORT="${TENANT_WEB_PORT}"
TENANT_POS_PORT="${TENANT_POS_PORT}"
TENANT_KDS_PORT="${TENANT_KDS_PORT}"
TENANT_CLIENT_PORT="${TENANT_CLIENT_PORT}"
TENANT_SUBDOMAIN="${TENANT_SUBDOMAIN}"
TENANT_JWT_SECRET="${TENANT_JWT_SECRET}"
TENANT_JWT_REFRESH_SECRET="${TENANT_JWT_REFRESH_SECRET}"
TENANT_CROSS_SECRET="${TENANT_CROSS_SECRET}"
MASTER_POSTGRES_PASSWORD="${MASTER_POSTGRES_PASSWORD}"
MASTER_REDIS_PASSWORD="${MASTER_REDIS_PASSWORD}"
